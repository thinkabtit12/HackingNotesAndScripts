nmap scanner command:
nmap -p- --min-rate 2000 -v <TARGET_IP>

nmap port information command:
nmap -sC -sV -p (port1),(port2 and so on) <TARGET_IP>

web server redirect finder command:
curl -I http://<TARGET_IP>

link DNS to IP command:
sudo nano /etc/hosts
(scroll to the very bottom of the file, paste your target IP, press Tab, and then paste the DNS you link it to.)

hidden subdomains finder command:
gobuster vhost -u http://(DOMAIN) -w /usr/share/wordlists/dirb/common.txt --append-domain
(every subdomain found here must be booked via "link DNS to IP command")
(using dir instead of vhost is also an alternative if you find no results using vhost)

check if targets running an active imap email service
nmap -p 143,993 <TARGET_IP>
(port 143 is standard imap, port 993 is secure imap)


universal raw byte hex analyzer command:
xxd <FILENAME> | head -n 20
(for when u cant read a file at all)

quick read command
head -n <LINE_COUNT> <FILE_NAME>

netcat start listen command
nc -lnvp <PORT>
